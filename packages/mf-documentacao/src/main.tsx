import React from "react";
import ReactDOM from "react-dom/client";
import Documentacao from "./Documentação";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Documentacao />
  </React.StrictMode>
);
